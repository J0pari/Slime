{
  "schema": "gpu-scheduler/v1",
  "fingerprint": "5d602cc2574cc3c26c8153d5bf4cfa09066f6ec902fb03aef0747792a509c690",
  "adoptedAt": "2026-09-14",
  "capabilities": ["contract", "status", "submit", "inspect", "progress/v1", "gpu-lock/v1"],
  "notes": "Slime Evolution pins the gpu-scheduler/v1 contract, now owned by commons (control/gpu_scheduler.py) after the control-plane migration; the ABI fingerprint is unchanged because owner/consumer registration is outside the pin scope. The client resolves the entrypoint at either home (control/ or src/) and recomputes the documented ABI scope when the owner's handoff module is absent. Adoption recorded against the commons home; the registry's next step is the smoke through it."
}
